apiVersion: v1
kind: ConfigMap
metadata:
  name: ms-operacao-config
  namespace: fgo-backend
  labels:
    app.kubernetes.io/name: ms-operacao
    app.kubernetes.io/part-of: fgo
data:
  # Configurações não-sensíveis — valores sensíveis ficam no Secret
  QUARKUS_LOG_LEVEL: "INFO"
  QUARKUS_LOG_CATEGORY_COM_EXAMPLE_LEVEL: "DEBUG"
  # URL pública da aplicação — usada pelo Swagger como server URL
  # Descubra com: oc get route ms-operacao -n fgo-backend -o jsonpath='{.spec.host}'
  APP_URL: "https://ms-operacao-fgo-backend.apps-crc.testing"
